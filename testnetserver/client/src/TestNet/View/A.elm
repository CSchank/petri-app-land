module TestNet.View.A exposing(..)
import TestNet.Static.Types.A exposing(Msg(..))
import TestNet.Static.Types exposing(A(..))
import Html exposing(Html)
import Debug exposing(todo)

view : A -> Html Msg
view a =
    todo "Please fill out the view function for the TestNet net for the A place."

title : A -> String
title a =
    todo "Please fill out the title function for the TestNet net for the A place."
